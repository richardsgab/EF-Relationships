@model OneToManyDemo.Models.ViewModels.CreateBoekViewModel

@{
	ViewData["Title"] = "Create";
}

<h2>Nieuw Boek Toevoegen</h2>
<div>
	<form asp-action="Create">
		@* security against cookies bad use *@
		@* @Html.AntiforgeryToken() *@
		<div class="form-group">
			<label asp-for="Titel" class="control-label"></label>
			<input asp-for="Titel" class="form-control"/>
			<span asp-validation-for="Titel" class="text-danger"></span>
		</div>
		<div class="form-group">
			<label asp-for="AuteurId" class="control-label"></label>
			<select asp-for="AuteurId" class="form-control" asp-items=
			"@(new SelectList(Model.Auteurs, "AuteurId", "Naam"))">
			</select>
		</div >
		<div class="form-group">
			<input type="submit" value="Create" class="btn btn-outline-info my-2" />
		</div>
	</form>
</div>

